The code cahnges the MAC address. The subprocess.run is available only in python 3
When the code was written Python 3 was the official version, but Python 2 was still available
To run the code use the following command:
sudo python3 main.py
